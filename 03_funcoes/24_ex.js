function potencia(base, potencia) {
    let expressao = base ** potencia;
    console.log(`O resultado da expressão ${base}^${potencia} é igual a: ${expressao}.`)
}

potencia(3, 2);
potencia(5, 5);
potencia(2, 3);